import { takeLatest, call, put } from 'redux-saga/effects';

function* handleRequestWSAgent() {
  // const WSAgents = yield call(getWebsocketAgent);
  //
  // if (WSAgents) {
  //   yield put(getWSAgentSuccess(WSAgents));
  // }
}

export default function* viewerSaga() {
  //yield takeLatest(requestWSAgent, handleRequestWSAgent);
}
